apiVersion: v1
kind: Service
metadata:
  name: mongodb-headless
  namespace: chapterone
spec:
  clusterIP: None
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: chapterone
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
